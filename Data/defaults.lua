-- Empty dataset shipped with the addon. The desktop client writes Data/db.lua
-- (gitignored) over these globals; that file is not in git so pull and checkout
-- never fight local raid data. CurseForge zips get an empty db.lua from CI.
wowauditTimestamp = nil
teamID = nil
difficulties = {}
wishlistData = {}
bonusRollTargets = {}
trinketPriorities = {}
